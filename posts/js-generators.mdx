---
title: Функции Генераторы
description: Подробно рассмотрим API для создания итераторов
date: '2022-12-25'
---
<Image
  alt={`Bridge`}
  src={`/images/iterators/Iterators_main.png`}
  width={497}
  height={301}
  priority
/>
## Особенности

> Итераторы итерируют <br/>Генераторы генерируют 🙃

Функции генераторы предоставляют нам интерфейс для итеративной генерации и манипулирования данными.<br />

Генераторы важно рассмотреть с двух сторон:
- что и как происходит внутри генератора
- что и как происходит снаружи генератора

## Что происходит внутри генератора

Немного терминологии:
- **Генераторная функция** или **Функция-генератор** - функция нового типа, всегда возвращающая объект генератор (Generator)
- **Объект Генератор** - объект, соответсвующий протоколу итератора, позволяющий получить доступ к генерируемым данным.

Давайте посмотрим на т.н. генераторную функцию:
```javascript
  function * genFunc() {
    console.log(1);
    yield 1;

    console.log(2);
    yield 2;

    console.log(3);
    const value = yield 3;

    return value;
  }
```

- `*` - спецсимвол, обозначающий что эта функция является генераторной
- `yield` - спецслово, на котором выполнение генератора останавливается до следующего вызова. При этом из генератора возвращается значение справа от yield. Если значения нету возвращается undefined
- `const value = yield` - конструкция, получающая значение извне генератора и записывающая ее в переменную `value`
- `return` - конструкция, показывающая окончание работы генератора. Может содержать значение(как `value` в данном примере). Последующие вызовы генератора будут возвращать пустой результат

Также важно понять как происходит выполение генератора

<Image
  alt={`Bridge`}
  src={`/images/js-generators/potok-vnutri-generatora.png`}
  width={800}
  height={600}
  priority
/>

### Наложение вызовов генератора
Сразу обратим внимание на то, как накладываются вызовы генератора. Это происходит, потому что ключевое слово `yield`
выполняет две важные фукнции:
- точка остановки - в этом месте генератор возвращает сгенерированное значение и останавливает свое исполнение
- точка возобновления - в этом месте генератор продолжает свое исполнение при следующем вызове.
### Возможность получить данные извне генератора
Следствие предыдущего пункта.<br />
`const value = yield 3;`<br />
т.к. `yield` является еще и точкой возобновления генератора, то в это место возобносления будет подставленно
отправленное нами в генератор значение, которое тут же будет присвоено переменной `value`.
### В генератор нельзя отправить данные при первом вызове генератора.
Это тоже логичный поинт, т.к. точкой возобновления является именно `yield`, отправить в генератор значение мы можем
только если он был остановлен в этой точке. Но если мы внимательно посмотрим на картинку то увидим, что первый вызов
генератора начинается не в точке `yield` а сначала функции. Т.о. у нас нету точки для считывания значения.
Эту проблему можно решить, добавив в генераторную функцию аргумент и передавая первое значение при создании
генератора, а не при первом его вызове.



## Похожесть с итераторами

Самое важное, что нужно понять генераторами - это их похожесть и отличия от итераторов. Поэтому давайте сразу начнем
с примера:

```javascript
  const iterator = [1,2,3][Symbol.iterator]();

  console.log(iterator.next());
  console.log(iterator.next());
  console.log(iterator.next());
  console.log(iterator.next());
  console.log(iterator.next());
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen02.png`}
  width={1598}
  height={855}
  priority
/>

Тут в целом все понятно:
1. Достаем итератор для массива из 3 элементов
2. Вызываем его 5 раз
3. Так как элементов всего 3, последние два вызова возвращаются пустыми

Давайте рассмотрим похожий пример с генератором:
```javascript
  function * generatorFunc() {
    yield 1;
    yield 2;
    return 3;
  }

  const generator = generatorFunc();

  console.log(generator.next());
  console.log(generator.next());
  console.log(generator.next());
  console.log(generator.next());
  console.log(generator.next());
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen03.png`}
  width={1103}
  height={547}
  priority
/>

Символ `*` дает интерпретатору понять, что это функция генератор <br />
Ключевое слово `yield` приостанавливает выполнение функции и возвращает текущее значение.

1. Сделали функцию генератор
2. Создали генератор
3. Вызвали его 5 раз
4. Так как значений всего 3, оставшиеся два вызова возвращаются пустыми.

Разберем как происходит работа внутри генератора:
1. При создании генератора никакой код внутри него не выполняется. Это похоже на создание объекта из класса - мы
тоже генерируем новую структуру на основе уже имеющейся, но ничего внутри нее не запускаем
2. Первый вызов `.next()` отрабатывает от начала функции до первого `yield` включительно.
3. Второй вызов отрабатывает все после первого `yield` до второго включительно
4. Третий вызов делает работу после второго `yield` до `return` включительно.
5. Все последующиее вызовы возвращают стандартный результать `{value: undefined, done: true}`

**Но, как водится, есть нюанс...**

Если мы внимательно посмотрим, как выглядит вывод последнего элемента для итератора и генератора, то увидим следующее:
```javascript
[
  // iterators
  {value: 3, done: false},
  // generators
  {value: 1, done: true}
]
```
Отличается параметр `done`. Семантически это логично, потому что мы возвращаем последнее значение при помощи `return` в
любой функции это показатель ее окончания. С генераторами все немного не так: по примеру мы видим, что функция может
быть вызвана еще сколько угодно раз, но результат всегда будет `{value: undefined, done: true}`

Главная проблема, которую это может породить - несовместимость с циклом `for..of`, который очень удобен для перебора
любого итератора и конечного генератора.<br/>
Для итератора:
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen04.png`}
  width={1789}
  height={552}
  priority
/>
Получаем вывод всех элементов

Для генератора
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen05.png`}
  width={1164}
  height={658}
  priority
/>
И мы видим, что последнее значение просто теряется🤷‍.

### Делаем генераторы итераторо-совместимыми
Я всегда выступаю за то, что бы генераторы были максимально похожи на итераторы, тем более что для этого не нужно
сильно напрягаться. В примере выше достаточно вынести последний элемент в `yield`, а `return` оставить пустым:
```javascript
  function * generatorFunc() {
    yield 1;
    yield 2;
    yield 3;

    return undefined;
  }

  const generator = generatorFunc();

  for (const value of generatorFunc()) {
    console.log(value);
  }
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen06.png`}
  width={790}
  height={646}
  priority
/>

Это не значит, что `return` в генераторе - это рудиментарная конструкция. На самом деле вам вполне может пригодится
вывести в конце какую-нибудь сервисную информацию или все сгенерированные элементы скопом - всё это будет работать в
случае перебора через `.next`, но не будет ломать код и логику при переборе через `for..of`

## Различие с итераторами

Главным отличием генератора от итератора является возможность получать данные извне на ходу:

```javascript
  function * doubleGen() {
    let value;

    while (true) {
      value = yield value * 2;
    }
  }

  const generator = doubleGen();
  console.log(generator.next(0));
  console.log(generator.next(1));
  console.log(generator.next(2));
  console.log(generator.next(3));
  console.log(generator.next(4));
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen07.png`}
  width={464}
  height={277}
  priority
/>
Разбираем по порядку:
1. Сразу отмечаем, что при первом вызове у нас ничего не получится в генератор. С одной стороны это странно - потому
что получается неполноценная логика взаимодействия. Но со стороны кода это абсолютно логично: первый вызов, как мы
помним отрабатывает от начала функции до первого `yield` включительно. Таким образом наш вызов `generator.next(0)`
на самом деле ничего внутрь генератора не передает. По-умолчанию `value` у нас = `undefined`, а конструкция
`undefined * 2` закономерно дает `NaN`(Not a Number).
2. Далее мы наблюдаем следующее:
  1. В момент вызова `yield` мы возвращаем из генератора выражение `value * 2`, при этом исполнение функции
останавливается
  2. В момент вызова `.next(<value>)` значение `<value>` подставляется на место вызова `yield` и функция продолжает
  исполнение до следующего, а именно срабатывает выражение `value = <value>`, после чего `while` идет на новый виток
    и снова останавливается когда мы возвращаем значение текущего `value * 2`

Не самое простое место для понимания 🙃 но вы справитесь.

Проблему с первым вызовом решается через "холостой" вызов `yield` в первый раз:

```javascript
  function * doubleGen() {
    let value;

    while (true) {
      value = yield value * 2;
    }
  }

  const generator = doubleGen();

  // холостой вызов
  generator.next();

  console.log(generator.next(0));
  console.log(generator.next(1));
  console.log(generator.next(2));
  console.log(generator.next(3));
  console.log(generator.next(4));
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen08.png`}
  width={475}
  height={291}
  priority
/>

Есть одно правило, которое я принял для себя, и советую вам тоже его придерживаться:<br />
**Всегда реализовывать холостой вызов для генераторов, в которые мы отправляем значения**.

Зачем и почему обсудим далее.

## Правило пользования генераторами

Чего-то принципиально нового я здесь не расскажу, но немного конкретизирую нюансы:

### Всегда делать генератор совместимым с API итератора(если это возможно)
Оговорка нужна, потому что мы не сможем сделать совместимым генератор, в который мы передаем значения.
