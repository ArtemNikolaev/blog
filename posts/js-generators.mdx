---
title: Функции Генераторы
description: Подробно рассмотрим API для создания итераторов
date: '2023-01-05'
---
<Image
  alt={`Bridge`}
  src={`/images/js-generators/logo.png`}
  width={768}
  height={500}
  priority
/>
## Особенности

> Итераторы итерируют <br/>Генераторы генерируют 🙃

Функции генераторы предоставляют нам интерфейс для итеративной генерации и манипулирования данными.<br />

Генераторы важно рассмотреть с двух сторон:
- что и как происходит внутри генератора
- что и как происходит снаружи генератора

## Что происходит внутри генератора

Немного терминологии:
- **Генераторная функция** или **Функция-генератор** - функция нового типа, всегда возвращающая объект генератор (Generator)
- **Объект Генератор** - объект, соответсвующий протоколу итератора, позволяющий получить доступ к генерируемым данным.

Давайте посмотрим на т.н. генераторную функцию:
```javascript
  function * genFunc() {
    console.log(1);
    yield 1;

    console.log(2);
    yield 2;

    console.log(3);
    const value = yield 3;

    return value;
  }
```

- `*` - спецсимвол, обозначающий что эта функция является генераторной
- `yield` - спецслово, на котором выполнение генератора останавливается до следующего вызова. При этом из генератора возвращается значение справа от yield. Если значения нету возвращается undefined
- `const value = yield` - конструкция, получающая значение извне генератора и записывающая ее в переменную `value`
- `return` - конструкция, показывающая окончание работы генератора. Может содержать значение(как `value` в данном примере). Последующие вызовы генератора будут возвращать пустой результат

Также важно понять как происходит выполение генератора

<Image
  alt={`Bridge`}
  src={`/images/js-generators/potok-vnutri-generatora.png`}
  width={800}
  height={600}
  priority
/>

### Наложение вызовов генератора
Сразу обратим внимание на то, как накладываются вызовы генератора. Это происходит, потому что ключевое слово `yield`
выполняет две важные фукнции:
- точка остановки - в этом месте генератор возвращает сгенерированное значение и останавливает свое исполнение
- точка возобновления - в этом месте генератор продолжает свое исполнение при следующем вызове.
### Возможность получить данные извне генератора
Следствие предыдущего пункта.<br />
`const value = yield 3;`<br />
т.к. `yield` является еще и точкой возобновления генератора, то в это место возобносления будет подставленно
отправленное нами в генератор значение, которое тут же будет присвоено переменной `value`.
### В генератор нельзя отправить данные при первом вызове генератора.
Это тоже логичный поинт, т.к. точкой возобновления является именно `yield`, отправить в генератор значение мы можем
только если он был остановлен в этой точке. Но если мы внимательно посмотрим на картинку то увидим, что первый вызов
генератора начинается не в точке `yield` а сначала функции. Т.о. у нас нету точки для считывания значения.
Эту проблему можно решить, добавив в генераторную функцию аргумент и передавая первое значение при создании
генератора, а не при первом его вызове.
### Особенность return
В функции генераторе `return` не завершает выполнение фукнции, как это происходит в классической функции. Но он даёт
информацию о том, что данные больше генерироваться не будут. Генератора можно вызывать после отработавшего `return`,
но он всегда будет возвращать `{value: undefined, done: true}`

## Что происходит все генератора
Начинается все с вызова генераторной функции:
```javascript
const generator = genFunc();
```
Эта строчка поможет нам понять что такое **генераторный метод** в отличие от **генератора**.<br />
Похожим образом отличается **итераторный метод** от **итератора**.<br />
Суть сводится к тому что вызывая метод genFunc() мы не выполняем ни строчки из описанного нами метода. Мы возвращаем
генератор, который уже будет последовательно выполнять содержимое `genFunc()`. Смысл в том, что мы можем
одновременно сгенерировать много генераторов, в каждый момент времени в каждом генераторе мы можем находиться на
разном этапе его исполнения. И это все должно работать не ломаясь 🙃.

```javascript
console.log(generator.next());
console.log('после первого шага');
console.log(generator.next());
console.log('после второго шага');
console.log(generator.next());
console.log('после третьего шага');
console.log(generator.next(12));
console.log(generator.next());
console.log(generator.next());
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen09.png`}
  width={736}
  height={637}
  priority
/>

В общем и целом все отрабатываем похожим на итератор способом, за исключением нескольких деталей:
1. Первая и главная - после третьего шага мы передаем в генератор значение - такого функционала от итератора вы
недождетесь 🙃. Думаю по этой же причине генераторы и вынесены в отдельный тип данных.
2. В момент вызова `return` в генератора мы получаем `{value: 12, done: true}`. Такого от итератора вы тоже не
дождетесь, потому что `done: true` будет возвращаться только после окончания всех итераций.

В связи с вышеперечисленными нюансами внутреннего и внешнего строения генераторов, я сформировал для себя несколько
правил, которых придерживаюсь при их написании.

## Правила написания генераторов
Главный вопрос, на который мы должны ответить - что мы пишем - итератор или генератор:
- В случае написания **итератора** мы полностью повторяем его API, это означает, что мы легко можем отправить его в
цикл `for..of` или перебрать его `*.next()` и ничто не даст нам понять, что на самом деле мы работаем с генератором,
а не итератором.
- Написание **генератора** подразумевает использование *главной фичи* - передачи значений извне-вовнутрь.

### Пишем итератор

**мы не передаем и не ожидаем передачи значений в генераторе**<br />
Логично, итераторы этого не умеют и это прямой отход от API итератора

**мы не передаем значение при помощи `return`**<br />
Выше мы немного коснулись этого вопроса, сейчас давайте коснемся подробнее.<br />
Допустим мы перебираем итератор массива `[1,2,3]`
B нас есть два главных способа это сделать:<br />
через `next`:
```javascript
const iterator = [1,2,3][Symbol.iterator]();

console.log(iterator.next());
console.log(iterator.next());
console.log(iterator.next());
console.log(iterator.next());
```
Четыре вызова, что бы увидеть пустой:
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen10.png`}
  width={547}
  height={225}
  priority
/>
Обратите внимание, что последний элемент содержит `done:false` и только следующий вызов возвращает `done:true`

или через `for..of`:
```javascript
for (const value of [1,2,3]) {
  console.log(value);
}
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen11.png`}
  width={114}
  height={154}
  priority
/>
В этом случае мы никогда не увидим последних вызовов, и в целом мы видим только `value` объекта `{value, done}`

Давайте предположим, что мы хотим сделать тоже самое при помощи генератора, по классике давайте начнем с
неправильного примера и поймем, почему он неправильный:
```javascript
function * gen() {
  yield 1;
  yield 2;
  return 3;
}
```
и выведем теми же двумя способами:
```javascript
const generator = gen();

console.log(generator.next());
console.log(generator.next());
console.log(generator.next());
console.log(generator.next());
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen12.png`}
  width={545}
  height={219}
  priority
/>
Уже можно заметить отличие в том, что для последнего значение `done: true`. Но почувствовать масштаб трагедии мы
сможем только в `for..of`

```javascript
for (const value of gen()) {
  console.log(value);
}
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen13.png`}
  width={102}
  height={109}
  priority
/>
Таким образом мы просто теряем последнее значение, потому что `for..of` останавливает исполнение, когда натыкается
на `done: true`.

**Правильным решением этой проблемы будет отказ от возвращения значений в `return`. Функция примет вид:
```javascript
function * gen() {
  yield 1;
  yield 2;
  yield 3;

  return undefined;
}
```
Таким образом мы обеспечиваем 100%-ную совместимость с API итератора.

### Пишем генератор

**Отправляем значения во все вызовы, кроме первого**<br />
Этот пункт навеян удобством использования, а не запретами языка. У нас нету какого-то подсчета вызовов и писать его
со всех сторон будет выглядеть глупо, все что у нас есть - это вызов `next()`, который подобную информацию не
предотавляет. Поэтому так важно сохранять **однотипность вызовов** - это значит, что если мы отправляем данные в
один вызов `.next()`, мы должны делать это со всеми вызовами `.next()`.<br />
Почему первый вызов является исключением мы рассмотрели выше - первый вызов генератора начинается от начала функции,
а не от `yield`. тогда как только `yield` способен прочитать отправленные данные.

**Если отправлять данные нужно единственный раз - отправьте их как аргумент генераторного метода и пишите
итератор**<br />

Да, генераторная функция может принимать аргументы. Выглядит это примерно так:

```javascript
function * gen(a) {
    yield 1;
    yield 2;
    yield a;
    return undefined;
}
for (const value of gen('bla')) {
    console.log(value);
}
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen14.png`}
  width={142}
  height={144}
  priority
/>

Благодаря этому мы сильно упростили себе жизнь, потому что если пытаться не нарушить предыдущий принцип, то пришлось
бы отправлять данные каждый раз и это был бы мрак заглушек 🙃
А так мы и данные отправили, и совместимость с API итератора сделали 🔥

**Делайте холостой вызов генератора сразу после его создания и пишите генераторный метод с расчетом на холостой
вызов**<br />
Первая часть достаточна несложна в понимании
```javascript
const gen = genFunc();
gen.next();

// some code

gen.next(1);
gen.next(...);
gen.next(n);
```
то бишь сразу делаем холостой вызов без значения, а потом уже в нужном месте сразу работаем с данными.<br />
Со стороны генераторного метода это выглядит примерно так:
```javascript
function * genFunc() {
  const value = yield;
  // do something with value
}
```
То бишь первый yield находится максимально близко к началу функции и сразу подготавливается переменная для второго
для записи данных из второго вызова функции.

Подобный подход к написанию итераторов и генераторов способен сильно улучшить читаемость и понятность вашего кода

## yield *
Ну и на-последок рассмотрим одну крутую особенность оператора `yield` - а именно возможность замыкать другие
итераторы и генераторы на себя, а после выполения оных возвращаться в поток текущего генератора 🤯

На примере это не должно быть сложным для понимания:
```javascript
function * genFunc1 () {
  yield 'a1';
  yield 'a2';
  yield 'a3';

  return undefined;
}
const arr = ['b1', 'b2', 'b3'];

function * genFunc2 () {
  yield 'c1';
  yield * genFunc1();
  yield 'c2';
  yield * arr;
  yield 'c3';

  return undefined;
}

for (const value of genFunc2()) {
  console.log(value);
}
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen15.png`}
  width={177}
  height={446}
  priority
/>
Врядли тут нужны какие-то комментарии, но тем не менее отметим, что в момент срабатывания `yield *` мы создаем
указанный генератор или итератор и идем по всем его значениям, и только после окончания возвращаемся в обычный поток.

## Как использовать генераторы
Давайте с вами решим задачу:
> IN: любое положительное целое число n
OUT: найти сумму всех чисел от 1 до n

Попробуйте решить эту задачу(хотя бы у себя в голове) перед тем как читать далее.

Не очень опытный разработчик написал бы что-то такое:
```javascript
function summ(n) {
  let result = 1;
  for (let i = 2; i <= n; i++) {
    result += i;
  }
  return result;
}
```
Более опытный разработчик, слазив в гугл выдаст это:
```javascript
function summ(n) {
  return n * (n + 1) / 2;
}
```
Оба решения верны, второе более оптимальное. Теперь давайте изменим условие задачи:
> При нажатии на кнопку выводить на экран сумму чисел от 1 до n где n - это количество нажатий на кнопку
Пример:
1-ое нажатие: 1
2-ое нажатие: 3
3-е нажатие: 6
n-ое нажатие: n * (n + 1) / 2

Обратите внимание, как изменилось условие. Если в первой задачей результатом является один вызов функции с числом
(потому что любой другой вызов уже будет проверкой другого условия), вторая задача не останавливает свое исполнение
никогда. Более того, промежутки между нажатиями могут быть любыми по желанию пользователя.<br />
Подобные звоночки и должны приводить нас к использованию генератора:
```javascript
function * summGenFunc() {
  let i = 1;
  let result = 0;
  while (true) {
    result += i;
    i++;
    yield result;
  }
}
```
<Image
  alt={`Bridge`}
  src={`/images/js-generators/gen16.png`}
  width={517}
  height={512}
  priority
/>

### Другие возможные примеры:
В одной из предыдущих статей я написал небольшую игру про [рыцаря в бездне](https://knight-in-the-gulf.vercel.app/)<br />
И я написал генерацию цветовой схемы для уровня используя генератор. Причины просты:
- во-первых **итеративность** - каждый уровень требует повторения генерации новой цветовой схемы
- во-вторых **отложенность во времени** - схема для следующего уровня генерируется только после прохождения
предыдущего, которое может быть бесконечным если ничего не делать.

Код выглядит так.
```javascript
export function* colorSchemeGeneratorFunc() {
  const schemes = [
    new Set(["#4186D3", "#FFB440"]),
    new Set(["#F56E8D", "#9CEF6C"]),
    new Set(["#FFB273", "#5CCCCC"]),
    new Set(["#FFCA40", "#3D4BB0"]),
    new Set(["#FFFF73", "#AD66D5"]),
    new Set(["#CD0074", "#9FEE00"]),
  ];

  while (true) {
    yield schemes[Math.floor(Math.random() * schemes.length)];
  }
}
```
то бишь, внутри у нас хранятся цветовые схемы, и среди них мы каждый раз случайным образом выбираем одну. Правильней
было бы засылать схемы извне, но так как игра маленькая и врядли будет сильно изменяться - оставим все как есть.

Ссылки для интересующихся:
[generator](https://github.com/ArtemNikolaev/game-knight-in-the-gulf/blob/68f32336849a4bbcf9016c41bc4fa67c92b55cf6/src/js/utils.js#L8)
[renderer](https://github.com/ArtemNikolaev/game-knight-in-the-gulf/blob/68f32336849a4bbcf9016c41bc4fa67c92b55cf6/src/js/Renderer.class.js#L33)

### Пример без кода

Давайте вспомним аркудную игру, в которой корабль отбивается от летящих в него камней:
<Image
  alt={`Bridge`}
  src={`/images/js-generators/asteroids.png`}
  width={474}
  height={276}
  priority
/>

Генерация этих камней(с разными размерами, скоростью, координатами появления) - хорошая работа для генератора.

---

## Вывод
Надеюсь вам было так же весело читать это, как мне это писать 😊 Искренне надеюсь, что ваш арсенал инструментов
Javascript пополнился такой мощной пушкой, как генераторы 😁 Отсылка тут не спроста, ей действительно лучше не
пытаться пулять по воробьям 🙃.
