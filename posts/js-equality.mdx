---
title: = == === (Javascript)
description: Небольшая заметка про присваивание, строгое и нестрогое равенство.
date: '2023-01-08'
---

## 🐦=🐦 Оператор присваивания
### Математика
В математике знак `=` означает *равенство* между левой и правой частью выражения. Это означает что и левая и правая часть выражения равноправны и равны одному и тому же.<br/>
То бишь `x=3` полностью равноправно с `3=x`. Забегая вперед скажу, что в Javascript это не так.

Но, что важнее, это понять что именно означает выражение `x=3`. Если ваш ответ *это означает, что икс равен трём*, то, как говорил преподаватель, садись два 🤯.<br/>
А означает это что:
> переменной `x` полностью соответствует число `3`

Про выражение `9=3x` можно сказать:
> произведению `3x` полностью соответствует число `9`

### Javascript
Попытка написать выражение `3=x` в JS будет ошибкой:
<Image
  alt={`Bridge`}
  src={`/images/js-equality/invalid_left-hand_side_in_assignment.png`}
  width={1019}
  height={105}
  priority
/>
Поэтому сразу делаем адекватный вывод:
> В javascript левая часть выражения не является равноправной и эквивалентной с правой частью выражения

Теперь давайте разберемся как же работает `=` в Javascript. Для этого нам понадобится два примера:
```javascript
// 1
let x = 3
// 2
x = 3
```
В первом случае мы создаем новую переменную, во втором перезаписываем уже когда-то созданную<br />
Правая часть выражения(справа от `=`) для этих двух примеров работает одинаково:
> Записывает в память `3` и возвращает ссылку на использованную ячейку памяти

**Правая часть выражения**(за исключением выражения `...=undefined`) всегда записывает какое-то значение в память. Не важно, находится там выражение типа `ax^2+b` или `48*12-56/42` или какое угодно еще - результат будет посчитан и записан в памяти и в выражение будет подставлена ссылка на использованную ячейку памяти.<br/> В javascript нет возможности напрямую работать со ссылками или указателями, они всегда запрятаны где-то внутри js и прикрываются переменными. С одной стороны это хорошо - язык за счет этого проще и понятнее. С другой стороны из-за этого возникают большие сложности с пониманием таких вещей как замыкание, определение функции, callback и прочее.

Теперь разберем **левую часть выражения**(слева от `=`).

В случае `let x = 3` интерпретатору дается задача создать сущность(переменную) в текущей области видимости, к которой можно обратиться по имени `x`.<br />
Также отметим, что для выражения `x=3`, если переменная `x` не была объявлена ранее, интерпретатору дается такая же задача на создание сущности, но уже в глобальной области видимости.<br/>
На всякий случай отметим, что для выражения:
```javascript
'use strict'
x=3;
```
Будет выдана ошибка, потому что строгий режим отрицает неявные(объявленные не в глобальной области видимости) переменные. 🤷‍♂️
<Image
  alt={`Bridge`}
  src={`/images/js-equality/use_strict_not defined.png`}
  width={773}
  height={87}
  priority
/>

Далее созданная сущность(переменная) сохраняет ссылку из правой части выражения. В случае, когда переменная уже существовала (`x=3`), ссылка заменяет ссылку, которая была записана в переменную ранее.<br/>
Таким образом, всякий раз, когда мы обращаемся к переменной(сущности) `x` в коде или в правой части выражения, мы извлекаем записанные по ссылке данные.<br />
Пару примеров:

`const y = x`
При выполнении **правой части выражения** интерпретатор, дойдя до `x` считает записанную в него ссылку и извлекает записанные по ней данные(`3`), после чего записывает их в другую ячейку и возвращает ссылку на эту ячейку.<br/>
Таким образом работают только простые(примитивные) типы данных типа `string` или `number`. С объектами, массивами и прочими референсными типами данных работа осуществляется по другому и это будет описано в отдельной статье про работу с памятью
**Левая часть выражения** создает в памяти сущность `y`, после чего записывает в нее ссылку, полученную от правой части выражения.

`const z = 3 * y + 2 * x`
Тут интереснее:<br/>
В **правой части выражения** интерпретатор сначала оценивает приоритет операторов, разбивает на очередь:
1.`3 * y` - считывает значение `y`, умножает на 3, получает 9 и записывает в память
2.`2 * x` - считывает `x`, умножает на 2, получает 6 и записывает в память
3. сумма предыдущих двух операторов - считывает из памяти результаты двух предыдущих операторов, суммирует, получает 15, записывает в память, возвращает ссылку на ячейку памяти.
В **левой части выражения** создается сущность `z` и в нее записывается ссылка из правой части выражения.

Таким образом оператор `=` можно называть **оператором присваивания**, потому как он всегда присваивает результат выполнения выражения справа переменной, указанной слева

## Операторы сравнения
Их два:
- `===` - оператор строгого сравнения
- `==` - оператор нестрогого сравнения

Важно, что они тоже не соответсвуют математическому оператору равенства `=`. Почему так давайте разбираться ниже

### 🐦===🐦 Оператор строгого равентсва
На уровне примитивных типов(литеральных) оператор соответсвует математическому:
```javascript
1 === 1 // true
1 + 2 === 3 // true
'bla' === 'bla' // true
12 === 6 // false
```
Но вот с референсными типами эта штука работать перестает:
```javascript
[] === [] //false
{} === {} // false
```
Перед тем, как объяснить почему, давайте пример с `true`:
```javascript
const a = []
a === a // true

const b = {}
b === b // true
```
Для тех, кто не догадался, оператор `===` требует полного совпадения прототипа, то бишь он буквально проверяет, что это тот же массив или объект.<br/>
Имейте это ввиду, когда будете его использовать 🙃

### 🐦==🐦 Оператор нестрогого равенства
Отличие от строгого равенства заключается в привидении типов, которое делает оператор в попытке сравнить несравнимое 🙃<br/>
На примере это выглядит так: допустим мы хотим сравнить строку `'3'` и число `3`.<br />
Строгое равенство `'3'===3 // false` в туже секунду нашу затею обламает. Однако нестрогое равенство попробует перевести строку в число и сделает строгое сравнение после. То бишь это будет выглядеть примерно так:
```
'3' == 3 // нестрогое равенство
Number('3') === 3 // строгое равентсво, которое делает тоже самое
```
То бишь нестрогое равенство - это строгое равентсво в котором сначала сделали приведение типов.🤷‍♂️
На MDN есть отличная [табличка](https://developer.mozilla.org/ru/docs/Web/JavaScript/Equality_comparisons_and_sameness), в которой подробно описано, какой тип куда приводится при сравнении одного с другим, рекомендую, полезно для ознакомления.
